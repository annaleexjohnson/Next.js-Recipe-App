import styles from "../styles/Header.module.css"

const Header = () => {
    return (
        <div className={styles.header}>
            <h1>flavorful</h1>
        </div>
    );
}

export default Header;